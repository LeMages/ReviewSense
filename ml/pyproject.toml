[project]
name = "ml"
version = "0.1.0"
description = "ReviewSense ML training pipeline"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.2.0",
    "scikit-learn>=1.5.0",
    "mlflow>=2.16.0",
]

[tool.uv]
package = false
